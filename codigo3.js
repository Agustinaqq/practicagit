/*Declare un nombre de función printArray. Toma matriz como parámetro e imprime cada va-
lor de la matriz.*/

function printArray(array) {
    //for (let i = 0; i < array.length; i++) {
      //  console.log (array[i]) }
        console.log(array)  }   

printArray([["dana"] ,true ,[12457],"abc",["tita"]])

let frutas = ["Manzana", "Banana"];

console.log(frutas);
frutas.forEach